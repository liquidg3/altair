define(['altair/facades/declare',
        'altair/mixins/_DeferredMixin'
], function (declare,
             _DeferredMixin) {

    return declare([_DeferredMixin], {

        next: function () {

        }

    });

});